# Copy this file to secrets.yaml (gitignored) and fill in the values.

# eLabFTW API key, used only as a fallback by the one-off scripts in scripts/.
# Server requests always act as the key the caller provides.
# Generate keys at https://eln.ddomlab.org/ucp.php?tab=3
eln_api_key: ""

# Token for the lab Slack bot (autofill error reports, peroxide reminders)
slack_bot_token: ""
